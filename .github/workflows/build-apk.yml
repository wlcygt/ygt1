name: Build Android Debug APK
on:
  workflow_dispatch:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 检出仓库文件
        uses: actions/checkout@v4

      - name: 解压压缩包
        run: unzip GR001.zip

      - name: 查看目录
        run: ls -R

      - name: 配置JDK17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: 进入源码目录编译
        run: |
          cd GR001
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: 上传APK制品
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: GR001/app/build/outputs/apk/debug/app-debug.apk
